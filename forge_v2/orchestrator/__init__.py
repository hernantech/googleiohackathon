"""Forge v2 orchestrator package.

Scaffold for the FastAPI + LangGraph service described in ../specs.
Currently only the connection layer + health endpoints are implemented;
graph nodes (specs/01), SME dispatch (specs/02) and the chat bus (specs/04)
fill in here.
"""

__version__ = "0.1.0"
